import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2670bf",
    },
    secondary: {
      main: "#19857b",
    },
    background: {
      default: "#121212",
    },
  },
});

export default theme;
